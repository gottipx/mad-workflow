interface:
  display_name: Agent Workspaces
  short_description: Use in Hermes Agent, OpenCode, or any coding-agent runtime to enforce one task, one branch, one isolated workspace, safe worktrees, and cleanup.
